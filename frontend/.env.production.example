# Скопируйте в .env.production на сервере перед npm run build:
#   cp .env.production.example .env.production
# Vite подхватывает VITE_* только на этапе сборки (не в рантайме).

VITE_API_URL=https://api.elearning.aqlant.com/api
